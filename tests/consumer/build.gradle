plugins {
    id "com.android.application" version "8.11.1"
    id "org.jetbrains.kotlin.android" version "2.2.20"
}
def withLegacySdk = providers.gradleProperty("withLegacySdk").getOrElse("false").toBoolean()
def withCompat = providers.gradleProperty("withCompat").getOrElse("false").toBoolean()
def withExtras = providers.gradleProperty("withExtras").getOrElse("false").toBoolean()
android {
    namespace = "eu.darken.porter.sdk.consumer"
    compileSdk = 36
    defaultConfig {
        applicationId = "eu.darken.porter.sdk.consumer"
        minSdk = 24
        targetSdk = 36
        versionCode = 1
        versionName = "1"
    }
    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_11
        targetCompatibility = JavaVersion.VERSION_11
    }
    buildTypes {
        release {
            minifyEnabled = true
            proguardFiles getDefaultProguardFile("proguard-android-optimize.txt"), "proguard-rules.pro"
            signingConfig = signingConfigs.debug
        }
    }
    if (withLegacySdk) {
        sourceSets.main.java.srcDir("src/legacy/java")
    }
    if (withExtras) {
        sourceSets.main.java.srcDir("src/extras/java")
    }
}
kotlin {
    compilerOptions {
        jvmTarget = org.jetbrains.kotlin.gradle.dsl.JvmTarget.JVM_11
    }
}
dependencies {
    implementation "com.github.d4rken-org.porter-api:sdk:${providers.gradleProperty('sdkVersion').getOrElse('0.2.0')}"
    if (withCompat) {
        implementation "com.github.d4rken-org.porter-api:shizuku-compat:${providers.gradleProperty('sdkVersion').getOrElse('0.2.0')}"
    }
    if (withLegacySdk) {
        implementation "dev.rikka.shizuku:api:13.1.5"
        implementation "dev.rikka.shizuku:provider:13.1.5"
    }
    if (withExtras) {
        implementation "com.github.d4rken-org.porter-api:sdk-extras:${providers.gradleProperty('sdkVersion').getOrElse('0.2.0')}"
    }
}
